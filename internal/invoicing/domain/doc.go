// Package invoicing contains the domains core and use cases (domain layer).
// Note that we have renamed it, to have avoid layering noise at all import sites and
// instead have real semantic prefix when referring to its identifiers.
package invoicing
