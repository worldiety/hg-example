// Package receiver has been introduced because the domain model has become to large, so we break into
// smaller and more independent parts.
// It is named receiver, because it contains all the stuff for handling invoice recipients.
package receiver
