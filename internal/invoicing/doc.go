// Package invoicing is an empty unidiomatic package, which represents the bounded context of invoicing.
// It contains the domain (core and use case), persistence and presentation layers.
package invoicing
