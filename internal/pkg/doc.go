// Package pkg contains implementations which are totally project independent and could possibly be extracted into
// standalone modules. See also the sys package for shared near-domain implementations.
package pkg
